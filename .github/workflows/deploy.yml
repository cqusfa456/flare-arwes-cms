name: Deploy to Cloudflare

on:
  push:
    branches: [next, main]
  workflow_dispatch:

# All credentials are stored as GitHub Actions secrets (never in the repo).
# Required GitHub secrets (set via: node scripts/setup-tui.mjs → GitHub step,
# or manually in Settings → Secrets and variables → Actions):
#   CF_API_TOKEN          — Cloudflare API token (Workers Scripts: Edit,
#                           Pages: Edit, D1, R2, KV permissions)
#   CF_ACCOUNT_ID         — Cloudflare account ID
#   CF_SITES_API_TOKEN    — (optional) dedicated token for the CMS runtime.
#                           Deploying pushes it into the Worker as CF_API_TOKEN
#                           so the CMS can manage domains, builds and the build
#                           environment. When unset the CI token above is used,
#                           which works but is broader than the CMS needs.
#                           Must be user-scoped (the Workers Builds API rejects
#                           account-scoped tokens) and carry Workers Builds
#                           Configuration: Edit, Workers Scripts: Read,
#                           Zone: Read and, for Pages sites, Pages: Edit.
#                           Create it with: node scripts/set-cf-token.mjs runtime
#   JWT_SECRET            — CMS auth secret
#   SCIFI_API_URL         — deployed CMS worker URL
#   SCIFI_API_TOKEN       — (optional) read-only CMS API token
#   PAGES_PROJECT_NAME    — (optional) Pages project name, default: arwes-docs
#
# The D1 database (sci-fi-cms-db), KV namespace (sci-fi-cms-cache) and — when R2
# is the media backend — the R2 bucket (sci-fi-cms-media) are created by this
# workflow when they are missing; see scripts/cf-resources.py. CF_API_TOKEN
# therefore needs D1, KV and R2 write permissions. The former
# CF_D1_DATABASE_ID / CF_KV_NAMESPACE_ID / CF_R2_BUCKET_NAME secrets are
# obsolete and ignored.
# Storage backend (optional — only when using an S3-compatible backend
# instead of R2). STORAGE_BACKEND selects the provider:
#   STORAGE_BACKEND=b2  → required: B2_BUCKET, B2_ACCESS_KEY_ID,
#                         B2_SECRET_ACCESS_KEY; optional: B2_ENDPOINT, B2_REGION
#   STORAGE_BACKEND=s3  → required: S3_BUCKET, S3_ACCESS_KEY_ID,
#                         S3_SECRET_ACCESS_KEY; optional: S3_ENDPOINT,
#                         S3_REGION, S3_FORCE_PATH_STYLE
# Either way the R2 binding is removed from the ephemeral wrangler.toml.

jobs:
  deploy-cms:
    name: Deploy CMS Worker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: cms/pnpm-lock.yaml

      # Resolve ONE credential for every wrangler call below.
      #
      # Two kinds of credential can drive this workflow:
      #   * CF_API_TOKEN      — a long-lived API token (preferred).
      #   * CF_REFRESH_TOKEN  — the OAuth refresh token from
      #     `node scripts/oauth-login.mjs`. It is exchanged for a fresh
      #     one-hour access token on every run, so unlike storing an access
      #     token there is no hourly expiry. It carries workers/d1/kv/pages
      #     write, which is everything the deploy needs; it can NOT mint API
      #     tokens, so the CMS runtime credential falls back to being unset.
      #
      # The OAuth path is used when CF_REFRESH_TOKEN is present; delete that
      # secret to go back to CF_API_TOKEN.
      - name: Resolve the Cloudflare credential
        env:
          CI_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          REFRESH_TOKEN: ${{ secrets.CF_REFRESH_TOKEN }}
          OAUTH_CLIENT_ID: 54d11594-84e4-41aa-b438-e81b8fa78ee7
        run: |
          set -uo pipefail
          credential=""
          source="none"

          if [ -n "${REFRESH_TOKEN}" ]; then
            echo "Exchanging CF_REFRESH_TOKEN for a fresh OAuth access token"
            credential=$(curl -s --max-time 60 -X POST https://dash.cloudflare.com/oauth2/token \
              -H 'Content-Type: application/x-www-form-urlencoded' \
              --data-urlencode grant_type=refresh_token \
              --data-urlencode "refresh_token=${REFRESH_TOKEN}" \
              --data-urlencode "client_id=${OAUTH_CLIENT_ID}" \
              | python3 -c 'import json,sys; print(json.load(sys.stdin).get("access_token",""))' 2>/dev/null || true)
            if [ -n "${credential}" ]; then
              source="oauth refresh"
            else
              echo "::warning::CF_REFRESH_TOKEN could not be exchanged (revoked?); falling back to CF_API_TOKEN."
            fi
          fi

          if [ -z "${credential}" ] && [ -n "${CI_API_TOKEN}" ]; then
            credential="${CI_API_TOKEN}"
            source="CF_API_TOKEN"
          fi

          if [ -z "${credential}" ]; then
            echo "::error::No usable Cloudflare credential. Set CF_API_TOKEN (node scripts/set-cf-token.mjs ci) or CF_REFRESH_TOKEN (node scripts/oauth-login.mjs)."
            exit 1
          fi

          echo "::add-mask::${credential}"
          echo "CF_CREDENTIAL=${credential}" >> "$GITHUB_ENV"
          echo "CF_CREDENTIAL_SOURCE=${source}" >> "$GITHUB_ENV"
          echo "Using the ${source} credential"

      - name: Install CMS dependencies
        working-directory: cms
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Build core
        working-directory: cms
        run: pnpm build

      - name: Build astro integration
        working-directory: cms/packages/astro
        run: pnpm build

      # Inject all private values from GitHub secrets into the local
      # (ephemeral) wrangler.toml for this build. Nothing is persisted.
      #
      # The D1 database, the KV namespace and — when R2 is the media backend —
      # the R2 bucket are resolved by NAME and created when missing
      # (scripts/cf-resources.py). Deploying onto a fresh account, or onto one
      # whose resources were deleted, therefore needs no manual step.
      #
      # The old CF_D1_DATABASE_ID / CF_KV_NAMESPACE_ID / CF_R2_BUCKET_NAME
      # secrets are obsolete and deliberately ignored: an id whose resource no
      # longer exists is exactly what used to fail this deploy with an opaque
      # binding error. Resources are addressed by name (D1_NAME / KV_NAME /
      # R2_NAME below).
      - name: Configure Cloudflare resources from secrets
        working-directory: cms/packages/cms
        env:
          # Exported so scripts/storage-config.py can read the selected provider.
          STORAGE_BACKEND: ${{ secrets.STORAGE_BACKEND }}
          B2_ENDPOINT: ${{ secrets.B2_ENDPOINT }}
          B2_BUCKET: ${{ secrets.B2_BUCKET }}
          B2_ACCESS_KEY_ID: ${{ secrets.B2_ACCESS_KEY_ID }}
          B2_SECRET_ACCESS_KEY: ${{ secrets.B2_SECRET_ACCESS_KEY }}
          B2_REGION: ${{ secrets.B2_REGION }}
          S3_ENDPOINT: ${{ secrets.S3_ENDPOINT }}
          S3_BUCKET: ${{ secrets.S3_BUCKET }}
          S3_ACCESS_KEY_ID: ${{ secrets.S3_ACCESS_KEY_ID }}
          S3_SECRET_ACCESS_KEY: ${{ secrets.S3_SECRET_ACCESS_KEY }}
          S3_REGION: ${{ secrets.S3_REGION }}
          S3_FORCE_PATH_STYLE: ${{ secrets.S3_FORCE_PATH_STYLE }}
          # wrangler authenticates with these; cf-resources.py creates the
          # resources with them too.
          CLOUDFLARE_API_TOKEN: ${{ env.CF_CREDENTIAL }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          D1_NAME: sci-fi-cms-db
          KV_NAME: sci-fi-cms-cache
          R2_NAME: sci-fi-cms-media
        run: |
          set -euo pipefail

          # Non-R2 storage backend (optional, from secrets). Removes the R2
          # binding and injects STORAGE_BACKEND + the provider's variables.
          if [ "$STORAGE_BACKEND" = "b2" ] || [ "$STORAGE_BACKEND" = "s3" ]; then
            python3 "${{ github.workspace }}/scripts/storage-config.py" wrangler.toml || exit 1
            export SKIP_R2=1
          fi

          python3 "${{ github.workspace }}/scripts/cf-resources.py" wrangler.toml || exit 1

          # JWT_SECRET for the production worker
          if [ -z "${{ secrets.JWT_SECRET }}" ]; then
            echo "::warning::JWT_SECRET secret not set. CMS auth will use a default value."
          fi
          echo "Cloudflare resources configured"

      # Smoke test only — this does NOT migrate production.
      #
      # Production migrations are applied by the CMS itself: the Worker bundles
      # every migrations/*.sql (src/db/migrations-bundle.ts, regenerated on
      # prebuild) and bootstrapMiddleware calls
      # MigrationService(c.env.DB).runPendingMigrations() on request, tracking
      # state in its own `migrations` table and tolerating "already exists" /
      # "duplicate column name" so re-runs are safe.
      #
      # Deliberately NOT using --remote here: that would add a second mechanism
      # with a separate tracking table (wrangler's d1_migrations) and make this
      # step a hard failure point, while the runtime path already owns schema.
      - name: Check migrations apply (local)
        working-directory: cms/packages/cms
        run: npx wrangler d1 migrations apply DB --env production
        env:
          CLOUDFLARE_API_TOKEN: ${{ env.CF_CREDENTIAL }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      - name: Deploy CMS to Cloudflare Workers
        working-directory: cms/packages/cms
        run: npx wrangler deploy --env production
        env:
          CLOUDFLARE_API_TOKEN: ${{ env.CF_CREDENTIAL }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      # Set the JWT_SECRET as an encrypted Worker secret (never in repo).
      - name: Set JWT_SECRET worker secret
        working-directory: cms/packages/cms
        run: |
          if [ -n "${{ secrets.JWT_SECRET }}" ]; then
            echo "${{ secrets.JWT_SECRET }}" | npx wrangler secret put JWT_SECRET --env production
          fi
        env:
          CLOUDFLARE_API_TOKEN: ${{ env.CF_CREDENTIAL }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      # Hand the deployed CMS the credentials it needs to drive Cloudflare at
      # runtime (custom domains, Workers Builds, the trigger's build
      # environment). Without these the Sites page can register sites and own
      # content, but every build/domain action reports "credentials are not
      # configured".
      #
      # The Worker resolves credentials environment-first
      # (CF_API_TOKEN / CF_ACCOUNT_ID) and only then falls back to D1 settings,
      # so this is the same mechanism as Admin → Sites → Save credentials —
      # just automated.
      #
      # The runtime token is scoped much more narrowly than the CI credential, so
      # it is minted here with scripts/create-cf-token.mjs (which needs API
      # Tokens: Write). CF_SITES_API_TOKEN remains an override for when the
      # operator supplies the runtime token themselves.
      #
      # An OAuth access token can never be installed on the Worker: it expires in
      # an hour. On that path either a scoped token is minted or the Worker is
      # left without one (and any credential from an earlier run is removed),
      # because a credential that dies in an hour is worse than none.
      - name: Set Cloudflare API worker secrets
        working-directory: cms/packages/cms
        env:
          # wrangler authenticates with these two; the step below is the one
          # that writes the *value* of CF_API_TOKEN onto the Worker.
          CLOUDFLARE_API_TOKEN: ${{ env.CF_CREDENTIAL }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          CREDENTIAL_SOURCE: ${{ env.CF_CREDENTIAL_SOURCE }}
          CI_API_TOKEN: ${{ env.CF_CREDENTIAL }}
          SITES_API_TOKEN: ${{ secrets.CF_SITES_API_TOKEN }}
          ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: |
          set -euo pipefail

          RUNTIME_TOKEN="${SITES_API_TOKEN}"
          if [ -z "$RUNTIME_TOKEN" ] && [ -n "$CI_API_TOKEN" ]; then
            echo "::group::Minting a scoped runtime token"
            # --print-token makes the value the only __TOKEN__ line, so nothing
            # else in the log can be captured by mistake.
            RUNTIME_TOKEN=$(CF_BOOTSTRAP_TOKEN="$CI_API_TOKEN" \
              node "${{ github.workspace }}/scripts/create-cf-token.mjs" runtime --print-token --ttl-days 365 \
              | sed -n 's/^__TOKEN__//p' | tail -1) || RUNTIME_TOKEN=""
            echo "::endgroup::"
          fi

          if [ -z "$RUNTIME_TOKEN" ] && [ "$CREDENTIAL_SOURCE" = "CF_API_TOKEN" ]; then
            echo "::warning::Could not mint a runtime token; using the long-lived CI token for the CMS runtime, which is broader than the CMS needs."
            RUNTIME_TOKEN="$CI_API_TOKEN"
          fi

          if [ -n "$RUNTIME_TOKEN" ]; then
            printf '%s' "$RUNTIME_TOKEN" | npx wrangler secret put CF_API_TOKEN --env production
          else
            echo "::warning::No Cloudflare runtime credential installed: the CMS cannot manage domains or builds until one exists. Fix by giving CF_API_TOKEN 'API Tokens: Write' so this step can mint a scoped token, or set CF_SITES_API_TOKEN (node scripts/set-cf-token.mjs runtime)."
            # Do not leave a stale/expiring credential behind.
            npx wrangler secret delete CF_API_TOKEN --env production \
              || echo "  (no CF_API_TOKEN secret present on the Worker)"
          fi

          if [ -n "$ACCOUNT_ID" ]; then
            printf '%s' "$ACCOUNT_ID" | npx wrangler secret put CF_ACCOUNT_ID --env production
          else
            echo "::warning::CF_ACCOUNT_ID is not set; the CMS will need the account id saved from Admin → Sites."
          fi

# NOTE: this workflow deploys ONLY the CMS Worker.
#
# Site builds are owned by the CMS itself: each site is registered in
# Admin → Sites with its hosting target (a Worker with static assets, or a
# Cloudflare Pages project), and building, domain binding and content ownership
# are driven from there. See cms/packages/core/src/services/sites.ts.
#
# The runtime credentials this workflow installs (CF_API_TOKEN /
# CF_ACCOUNT_ID) are what let the Sites page talk to Cloudflare. Nothing else has
# to be pasted by hand.
#
# Previously a `deploy-docs` job built ./apps/docs and ran `wrangler pages
# deploy` here. It was removed so that all sites — their build config, domain
# bindings and content — are managed from one control plane instead of being
# spread across this workflow.
#
# Setup required once per site in Cloudflare. For the docs Worker
# (apps/docs/wrangler.jsonc, name `arwes-docs-worker`):
#   1. Make the Worker exist and connect it to this Git repository:
#      Worker → Settings → Builds.
#   2. In Admin → Sites press "Import Arwes presets" to register it (or register
#      manually), then press "Push build config + env". That installs
#        build_command  : sh ./apps/docs/scripts/build-worker.sh
#        deploy_command : cd apps/docs && ../../cms/packages/cms/node_modules/.bin/wrangler deploy
#        root_directory : /
#      and the build environment (PUBLIC_SCIFI_API_URL, PUBLIC_SCIFI_SITE and a
#      read-only PUBLIC_SCIFI_API_TOKEN scoped to that site) on the trigger, so a
#      build knows which CMS and which site it is building for.
#   3. Press "Build now". Worker sites build through the Builds API
#      (POST .../builds/triggers/{uuid}/builds), so no Deploy Hook is required;
#      creating one and pasting its URL into the site stays available as a
#      fallback. Pages sites still require a Deploy Hook (Direct Upload projects
#      cannot be rebuilt by one).
#   4. Attach custom domains from Admin → Sites. A Worker can serve many
#      hostnames; the CMS binds them through the Workers custom-domains API and
#      rejects a hostname that is not inside a zone the token can see.

